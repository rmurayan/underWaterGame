import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  logo: {
    width: 400,
    height: 200,
    resizeMode:"contain"
  },
  playButton: {
    marginTop: 32,
    width: 200,
    height: 120,
    resizeMode:"contain"

  }
})
